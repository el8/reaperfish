To use the Makefile put the following in docode/bin/build.sh:

#!/bin/bash
echo "building app in docker container"
cd /cthulhu
source .env.sh

export GO111MODULE=off
go build -ldflags="-X do/doge/version.commit=$(date +%s) -X do/doge/version.gitTreeState=dirty" do/teams/systems/reaper/cmd/reaper/
if [ $? -eq 0 ]; then
        echo "success"
fi


llvm/clang packages
--------------------

libllvm10:amd64                                 install
libllvm6.0:amd64                                install
llvm-10                                         install
llvm-10-dev                                     install
llvm-10-runtime                                 install
llvm-10-tools                                   install
clang-10                                        install
libclang-common-10-dev                          install
libclang-cpp10                                  install
libclang1-10                                    install

sudo update-alternatives --install /usr/bin/llvm-strip llvm-strip /usr/bin/llvm-strip-10 10
sudo update-alternatives --install /usr/bin/clang clang /usr/bin/clang-10 10
sudo update-alternatives --install /usr/bin/llc llc /usr/bin/llc-10 10
